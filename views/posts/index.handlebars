<div class="card">
    <div class="card-body">
        <h2>{{post.title}}</h2>
        <hr>
        <p>{{post.content}}</p>
        <hr>
        <small>Data de criação: {{post.date}}</small>
    </div>
</div>